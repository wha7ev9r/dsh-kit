/**
 * Host half of the Firefly theme bundle.
 *
 * The theme is rendered by the Client half. Keeping a no-op Host export makes
 * this package a normal DSH bundle without adding Host-only services.
 */
export function apply() {}
